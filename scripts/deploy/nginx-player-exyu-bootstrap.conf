server {
    listen 80;
    listen [::]:80;
    server_name player.exyu.tv;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 503 "player.exyu.tv TLS bootstrap in progress\n";
    }
}
